<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Jeonwoojo" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">#1A237E</item>
        <item name="android:statusBarColor">#1A237E</item>
    </style>

    <style name="Theme.Jeonwoojo.WithActionBar" parent="Theme.Material3.Light">
        <item name="colorPrimary">#1A237E</item>
        <item name="android:statusBarColor">#1A237E</item>
    </style>

    <!-- 메인화면 3개 버튼 공통 스타일 (이미지 디자인 재현) -->
    <style name="MainButton" parent="Widget.Material3.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">80dp</item>
        <item name="android:layout_marginStart">24dp</item>
        <item name="android:layout_marginEnd">24dp</item>
        <item name="android:textSize">26sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="cornerRadius">4dp</item>
    </style>
</resources>
